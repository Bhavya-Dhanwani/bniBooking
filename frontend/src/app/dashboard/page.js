import UserDashboard from "@/features/dashboard/UserDashboard";

export const metadata = {
  title: "Dashboard | BNI Kutch Event Booking",
};

export default function DashboardPage() {
  return <UserDashboard />;
}
